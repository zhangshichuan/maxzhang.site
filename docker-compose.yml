services:
  migrate:
    build:
      context: .
      dockerfile: Dockerfile.prisma
    volumes:
      - sqlite_data:/app/data
    environment:
      - DATABASE_URL
    profiles:
      - migrate

  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - sqlite_data:/app/data
    environment:
      - DATABASE_URL
    ports:
      - '3000:3000'

# 查看真实路径：docker volume inspect maxzhangsite_sqlite_data
volumes:
  sqlite_data:
