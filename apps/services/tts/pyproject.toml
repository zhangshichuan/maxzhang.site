[project]
name = "maxzhang-tts"
version = "1.0.0"
description = "流式 TTS 合成服务（edge-tts）"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.34",
  "edge-tts>=7.0",
]

[tool.uv]
package = false
