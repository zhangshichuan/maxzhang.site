# TTS 服务（FastAPI）本地环境变量（可选）
# 复制为 apps/services/tts/.env 使用；.env 已被 gitignore，不会提交。

# TTS_CACHE_DIR=./.cache/tts
# TTS_CACHE_MAX_BYTES=1073741824
# TTS_CACHE_MAX_AGE_SECONDS=2592000
# TTS_CLEANUP_INTERVAL_SECONDS=600
# TTS_MAX_TEXT_LENGTH=60000
